IMMEDIATE TODO:
	documentation

box/
	private.key
	public.key
	config


IMPORTANT LINKS:
	[link] http://www.laurentluce.com/posts/python-and-cryptography-with-pycrypto/
	[description] practical explanation and examples with pycrypto

GIT COMMANDS:
	git pull (pull)
	git push -u origin master (push)
	git commit -m "Some message here" -a (commit all changes)
	git add . (add all changes)
        git rm --cached filename (untrack files)

QUICK SETUP (for testsite):
	<install python django>
	easy_install pycrypto
	easy_install --upgrade pycrypto
	python secure_start.py (in testsite dir)
